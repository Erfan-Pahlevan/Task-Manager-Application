write a middleware and import it and call it whenever it is required both global and local
private usage: it should consist of windowMs and get a limit
whenever it bcame a local middleware, it should replace a local middleware